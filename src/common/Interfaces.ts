export interface DogZipCode {
  zipcode: string[];
}
export interface Dog {
  img: string;
  name: string;
  age: number;
  zip_code: string;
  breed: string;
}

export interface Location {
  city: string;
  county: string;
  latitude: string;
  longitude: string;
  state: string;
}
